'use strict'

// describe('POST /registration', () => {
//   it('should allow valid input to create a new user')
//   it('should not allow duplicate usernames to be created')
//   it('should not allow duplicate emails to be used')
//   it('should require a username, password, and email')
//   it('should not allow registrations to set isAdmin')
//   it('should nto allow registrations to set isActive')
//   it('should set isAdmin to false for all new registrations')
//   it('should set isActive to true for all new registrations')
// })
